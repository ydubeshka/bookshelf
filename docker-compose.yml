services:
  db:
    image: postgres:15-alpine
    container_name: bookshelf_pg
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "5432:5432"
    volumes:
      - bookshelf_pg_data:/var/lib/postgresql/data

volumes:
  bookshelf_pg_data: